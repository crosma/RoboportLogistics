config = {
	--can be red or green, of course
	wire_color = defines.circuitconnector.red,
	
	--how many ticks to wait between updates (60 ticks = 1 second) the 
	--higher this is, the less CPU it will use but the higher chance 
	--that the output to inserts will be incorrect
	tick_interval = 10,
}